apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgres
  name: postgres
spec:
  type: ClusterIP
  ports:
  - name: "postgres-service"
    port: 5432
    targetport: 5432
  selector:
    app: postgres